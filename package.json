{
  "name": "watermarknt-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for watermark removal",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}